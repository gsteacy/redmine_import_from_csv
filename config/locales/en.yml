# English strings go here for Rails i18n
en:
  Import_label: "Import issues from CSV"
  error_bad_csv_format: "Bad file format"
  format_hint: "The first line of the file should contain headings."
  required_fields: "Mandatory fields: Subject, Tracker, Author"
  optional_fields: "Optional fields: Description, Assignee, Status"
